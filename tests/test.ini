[DEFAULT]
some_key = some_value

[project.local]
local_path = will_be_replace_during_test
filter = repo*
type = TEST
gitweb_base_url = https://gitlab.com/some/repo/$name

[project.remote]
group = vino9
type = API1
gitweb_base_url = https://gitlab.com/some/repo/$name

