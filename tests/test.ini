[DEFAULT]
some_key = some_value

[project.local]
local_path = will_be_replace_during_test
filter = repo*
type = TEST
gitweb_base_url = https://gitlab.com/some/repo/$name

[project.remote]
group = vino9
type = API1
gitweb_base_url = https://gitlab.com/$name
gitlab_url = https://www.gitlab.com
ssl_verify = yes

[project.remote2]
group = vino9
type = API1
gitweb_base_url = https://gitlab.com/$name
gitlab_url = https://www.gitlab.com
ssl_verify = yes
# bad filter, won't match anything
filter = blah